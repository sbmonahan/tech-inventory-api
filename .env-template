# ── EXISTING ────────────────────────────────────────────────────────────────
PACT_BROKER_BASE_URL=https://<your-org>.pactflow.io
PACT_BROKER_TOKEN=<your-pactflow-token>
PACT_BRANCH=main

SH_OWNER=<your-swaggerhub-org>
SH_API=<your-api-name> # Example: tech-inventory-api
SH_API_KEY=<your-swaggerhub-api-key>

SH_BASE_URL=https://api.swaggerhub.com
SH_RESOLVED=true   # set to true if you want $refs resolved
SH_FLATTEN=true    # set to true to flatten

DEBUG_FETCH=1


# ── ADDED: Provider verification & outputs ──────────────────────────────────
# Provider identity and where your API is running locally
PROVIDER_NAME=<your-provider-name> # Example: tech-inventory-api
PROVIDER_BASE_URL=<your-api-url> # Example: http://localhost:3000

# Version metadata recorded with the verification result in PactFlow
# (set per run; format whatever you like)
# PROVIDER_APP_VERSION=2.4.0-main.123

# Where to write verification artifacts (summary.json, summary.txt, junit.xml, logs)
PACT_OUT_DIR=<your-pact-file-directory> # Example: ./artifacts/pact-verification
VERIFIER_LOG_TO_FILE=true
PACT_LOG_LEVEL=info           # debug | info | warn | error


# ── ADDED: Consumer selection for provider verification (optional) ─────────
# By default, the verifier uses the latest pacts from PACT_BRANCH (main).
# You can override with a specific consumer branch and/or tag, or provide full selectors JSON.

# PACT_CONSUMER_BRANCH=main
# PACT_CONSUMER_TAG=staging

# Full control (overrides the two above if set): JSON array per Pact selectors schema
# Example: latest on main and latest with tag=beta
# PACT_CONSUMER_SELECTOR_JSON=[{"branch":"main","latest":true},{"tag":"beta","latest":true}]


# ── ADDED: Broker features (optional) ───────────────────────────────────────
PUBLISH_VERIFICATION_RESULT=true
PACT_ENABLE_PENDING=false
# PACT_INCLUDE_WIP_SINCE=2024-01-01


# ── OPTIONAL: Consumer publishing convenience (set when you want) ──────────
# CONSUMER_APP_VERSION=1.0.2-bugfix-123
# PACT_CONSUMER_DIR=./pacts
